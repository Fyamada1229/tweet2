<div class="contents row">
  <p><%= @nickname %>さんの投稿一覧</p>

  <% @tweets.each do |tweet| %>
    <%= render partial: "tweets/tweet", locals: { tweet: tweet } %>
  <% end %>
  
  <%= paginate(@tweets) %>
</div>

<div class="top_go">
    <%= link_to 'トップページ', root_path, class: "top_go" %>
</div>

